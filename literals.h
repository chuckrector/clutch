#if !defined(LITERALS_H)

#define CHANGE_REQUIRES_RECOMPILE \
    "This is an arbitrarily imposed limit of a reasonable size and could be increased " \
    "if desired.\nThis currently would require recompiling Clutch.\n"

#define LITERALS_H
#endif